# Очистка проекта: удалены веб-компоненты и inference

## ✅ Удалено

### Веб и серверы:
- `inference/server/ws.py` - WebSocket сервер
- `tests/test_tcp_server.py` - тест TCP сервера
- `websocket/` - пустая директория

### Inference скрипты:
- `inference/test_loop.py` - тестовый цикл
- `inference/test_reference.py` - создание референсов
- `inference/test_reference_create.py` - создание референсов
- `inference/load_model.py` - загрузка моделей для inference
- `inference/get_last.py` - поиск последней модели

### Зависимости:
- `websockets` - больше не нужен без веб-сервера
- `pytest-asyncio` - не используется

## 📊 Что осталось

### Обучение:
- `train_sb3.py` - основной скрипт обучения с SB3
- `callbacks/` - кастомные callbacks для обучения
- `config/` - конфигурация обучения и окружения

### Окружение:
- `env/` - окружение дрона и обертки
- `utils/generation.py` - генераторы сценариев

### Графики и визуализация:
- `utils/save.py` - функции для построения графиков:
  - `save_rewards()` - график наград
  - `plot_episode_summary()` - визуализация эпизодов
  - `save_critic_loss()` - график loss критика
  - `save_actor_loss()` - график loss актора
  - И другие функции визуализации

### Логирование:
- `utils/db.py` - логирование в DuckDB
- TensorBoard логи (автоматически через SB3)

### Тесты:
- `tests/test_env.py` - тесты окружения
- `tests/test_env_obs.py` - тесты наблюдений
- Другие unit-тесты

## 🎯 Результат

Проект теперь сфокусирован только на:
1. **Обучении** - через `train_sb3.py`
2. **Построении графиков** - через функции в `utils/save.py` и TensorBoard
3. **Логировании** - в БД и TensorBoard

Все веб-компоненты и inference скрипты удалены.
