# Очистка неактуального кода после рефакторинга

## ✅ Файлы, которые можно безопасно удалить

Эти файлы полностью заменены функциональностью из Stable-Baselines3:

1. **`agents/ddpg_agent.py`** - заменен на `stable_baselines3.DDPG/TD3`
2. **`agents/maddpg_agent.py`** - заменен на SB3 (пока не используется)
3. **`networks/actor.py`** - заменен на стандартные MLP из SB3
4. **`networks/critic.py`** - заменен на стандартные MLP из SB3
5. **`utils/per_replay_buffer.py`** - заменен на встроенный replay buffer из SB3
6. **`train_single_agent.py`** - заменен на `train_sb3.py`
7. **`train_multi_agent.py`** - пустой файл

## ⚠️ Файлы, которые нужно обновить перед удалением

Эти файлы используют старый код и требуют обновления:

1. **`inference/load_model.py`** - использует `DDPGAgent`
   - Нужно обновить для загрузки моделей из SB3
   
2. **`utils/save.py`** - импортирует `DDPGAgent` в функции `save_checkpoint`
   - Функция `save_checkpoint` не используется в новом коде (SB3 делает это автоматически)
   - Можно удалить импорт и функцию, если она не используется

3. **`utils/warmup.py`** - использует старый агент для warm-up
   - Не используется в `train_sb3.py` (SB3 имеет `learning_starts` параметр)
   - Можно удалить или обновить для SB3

4. **`tests/test_env.py`** и **`tests/test_env_obs.py`** - импортируют `DIRECTIONS` из `ddpg_agent`
   - Нужно проверить, что такое DIRECTIONS и заменить на константу

## 📋 План действий

### Шаг 1: Удалить безопасные файлы
```bash
rm agents/ddpg_agent.py
rm agents/maddpg_agent.py
rm networks/actor.py
rm networks/critic.py
rm utils/per_replay_buffer.py
rm train_single_agent.py
rm train_multi_agent.py
```

### Шаг 2: Обновить зависимые файлы
- Обновить `inference/load_model.py` для работы с SB3 моделями
- Удалить неиспользуемый код из `utils/save.py`
- Обновить или удалить `utils/warmup.py`
- Исправить тесты

### Шаг 3: Проверить, что все работает
- Запустить `train_sb3.py`
- Проверить inference
- Запустить тесты

## 📊 Статистика

- **Удаляется**: ~800 строк самописного кода
- **Заменено на**: ~100 строк использования библиотеки
- **Экономия**: ~700 строк кода для поддержки
